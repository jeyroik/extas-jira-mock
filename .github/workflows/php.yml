name: PHP Composer

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        php-version:
          - "7.4"

    steps:
    - uses: actions/checkout@v2

    - name: "Install PHP with extensions"
      uses: "shivammathur/setup-php@v1"
      with:
       php-version: "${{ matrix.php-version }}"
       coverage: "pcov"

    - name: Validate composer.json and composer.lock
      run: composer validate

    - name: Install dependencies
      run: composer install --prefer-dist --no-progress --no-suggest

    - name: Copy .env
      run: cp tests/.env .env

    - name: MongoDB in GitHub Actions
      uses: supercharge/mongodb-github-action@1.1.0

    - name: Install mongodb php-extension
      run: sudo apt install php7.4-mongodb

    - name: Run php server
      env:
        EXTAS__JIRA_MOCK__HOST: test
        EXTAS__JIRA_MOCK__BASE_PATH: $PWD/tests
      run: php -S 0.0.0.0:8080 -t src/web > /server.log 2>&1 &

    - name: Try server
      run: curl 0.0.0.0:8080//rest/api/2/issue

    - name: Extas install
      run: vendor/bin/extas i

    - name: Run test suites
      run: composer run-script test

    - name: "Send code coverage report to Codecov.io"
      env:
        CODECOV_TOKEN: "${{ secrets.CODECOV_TOKEN }}"
      run: "bash <(curl -s https://codecov.io/bash) || true"